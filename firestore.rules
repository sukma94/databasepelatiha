rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // All collections are open for read/write.
    // Admin page protection is handled at the application level
    // via Firebase Auth + ProtectedRoute in the React app.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
